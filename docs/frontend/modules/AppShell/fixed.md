# Fixed

- `APPSHELL-FIX-001` — Giảm độ âm của `letter-spacing` và tăng `line-height` cho heading/card heading để tiếng Việt không bị dính chữ. Đã xác nhận lại bằng Computer Use trên route `/app`.
- `APPSHELL-FIX-002` — AppShell Vue có sidebar responsive, parent menu/dropdown, active route, mobile drawer/backdrop và nhóm điều hướng theo navigation map trong docs.
- `APPSHELL-FIX-003` — Thêm route fallback `/app/beginner/:stage` để `continueTarget` từ Learning không tạo dead link.
- `APPSHELL-FIX-004` — Active state của Trang chủ chỉ khớp chính xác `/app`; các route con không còn làm mất trạng thái active của mục hiện tại. Đã retest bằng Computer Use.
- `APPSHELL-FIX-005` — Đồng bộ AppShell Vue với `design-template` panda theme: dùng logo asset thật, token xám/đen, cấu trúc sidebar/header/app overview và breakpoint drawer 820px; giữ nguyên API và route behavior. Đã đối chiếu Computer Use ở viewport desktop và breakpoint mobile.
- `APPSHELL-FIX-006` — Chuẩn hóa learner screens theo content-first: bỏ intro dư, thay breadcrumb phân cấp bằng một nhãn route ngắn, khai báo `meta.title` cho route generic và giảm top padding xuống 14px desktop / 12px mobile. Giữ nhãn dashboard `TỔNG QUAN HỌC VIÊN`, nội dung chính, sidebar, màu và luồng.
- `APPSHELL-FIX-007` — Đưa nút beginner vào thanh tiêu đề danh sách; giữ metadata và mũi tên hàng học cùng một dòng bằng selector chuyên biệt.
- `APPSHELL-FIX-008` — Thay `PageView` placeholder bằng các màn hình content-first theo route cho bài học, kỹ năng, từ vựng, chữ Hán, ngữ pháp, ôn tập và các nhóm mở rộng; giữ tiêu đề ngắn, action cạnh nội dung và không thêm PageHeader/PageHero blocking. Các mục chưa có route thật được render dạng tĩnh, không còn link giả `#`.
- `APPSHELL-FIX-009` — Parent submenu luôn mở khi một child route đang active, kể cả khi state UI còn lưu `false`; tránh mất nhãn sau khi chuyển route hoặc refresh.
- Computer Use: desktop 1440px — `/app`, `/app/review`, `/app/beginner`; mobile 390px — `/app/vocabulary`, `/app/pinyin`, `/app/practice/vocabulary`, `/app/beginner`. Tiêu đề route chính xác, intro/breadcrumb dư không còn, CTA và hàng học không clipping; dấu tiếng Việt/chữ Hán hiển thị đúng.
- Computer Use bổ sung: desktop `/app/lessons`; mobile `/app/skills/listening` và mở drawer/sidebar — pass; sidebar giữ đầy đủ nhãn và active route.
- Computer Use 2026-09-22: `/app/skills/listening` → `/app` → mở lại nhóm Kỹ năng → `/app/skills/listening` → refresh; parent expanded và toàn bộ child labels vẫn hiển thị — pass.
- `APPSHELL-FIX-010` — Thay context HSK tĩnh trong sidebar bằng selector lấy dữ liệu từ `/api/curriculum/hsk-levels` và đồng bộ với `/api/learning/home`; lựa chọn gọi `/api/learning/hsk/{level}/select`, giữ nguyên route và vẫn có fallback HSK 3 khi API chưa sẵn sàng.
- Computer Use 2026-09-22: desktop `/app` — selector `Chọn cấp độ HSK` hiển thị `HSK 3`, menu mở/đóng được, chọn lại HSK hiện tại không đổi route; sidebar vẫn giữ đầy đủ group/label, không mất chữ tiếng Việt/chữ Hán — pass.
- `APPSHELL-FIX-011` — Chuẩn hóa `Kỹ năng` và `Luyện tập` thành parent dropdown-only trong production Vue; route cũ `/app/skills` và `/app/practice` chỉ còn redirect tương thích tới child cụ thể, không render page tổng quan/header dư. Dashboard card thứ tư chuyển thành context tĩnh, không còn link vào page Kỹ năng dư.
- `APPSHELL-FIX-012` — Sidebar lấy số cấp độ từ `selectedHskLevel.levelNumber` thay vì hardcode `3`; khi API trả HSK 1, biểu tượng, nhãn và lựa chọn đều hiển thị HSK 1. Computer Use retest trên `/app/skills/speaking` — pass.
- `APPSHELL-FIX-013` — Dọn cấu hình core route đã chết khỏi `PageView.vue`; các màn Kỹ năng, Luyện tập, Lộ trình, Từ vựng, Chữ Hán, Ngữ pháp và Ôn tập chỉ còn render từ view production tương ứng. `PageView` chỉ giữ route mở rộng và Cài đặt/Trợ giúp, tránh duy trì hai nguồn layout cho cùng một màn.
- Computer Use 2026-09-23: `/app/stories?qa=pageview-single-source-20260923` vẫn tải content published `Một ngày ở lớp học` sau khi dọn cấu hình — pass.
- Automated: `npm run build` — pass; `git diff --check` — pass.
