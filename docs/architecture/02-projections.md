# Projections

Inline projection:
- dữ liệu cần consistency ngay;
- current exam state;
- read model cần trả ngay sau command.

Async projection:
- thống kê;
- điểm yếu;
- lịch sử tổng hợp;
- báo cáo;
- progress dài hạn.

Không dùng Async cho dữ liệu mà UI cần ngay sau khi command thành công.
